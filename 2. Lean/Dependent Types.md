- the **return type** can depend on the **value** of an argument
- the return type is `Nat` when `s` is `pos`, and `Int` when `s` is `neg`
- the type itself changes based on the input value

![[dependent types example.png]]